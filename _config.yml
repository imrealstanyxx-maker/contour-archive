# GitHub Pages configuration for Jekyll (если используется)
# Но так как это статический сайт, можно оставить пустым или удалить

plugins:
  - jekyll-sitemap

# Отключаем Jekyll для чистого статического сайта
theme: null

