# 📋 Инструкция по развёртыванию КОНТУР на GitHub

## 🎯 Цель

Сделать сайт общедоступным через GitHub Pages с URL, содержащим название компании для максимального погружения.

## ⚡ Быстрый способ (5 минут)

### 1. Установите Git
Скачайте и установите: https://git-scm.com/download/win

### 2. Создайте репозиторий на GitHub

1. Зайдите на https://github.com
2. Нажмите **"+"** → **"New repository"**
3. **Название**: `contour-archive`
   - Это даст URL: **`contour-archive.github.io`** ✅
   - Или: `kontur-archive` → `kontur-archive.github.io`
4. Выберите **Public**
5. **НЕ** добавляйте README, .gitignore, license
6. Нажмите **"Create repository"**

### 3. Запустите скрипт деплоя

В PowerShell в папке проекта выполните:

```powershell
.\deploy.ps1
```

Скрипт:
- Проверит наличие Git
- Инициализирует репозиторий (если нужно)
- Добавит все файлы
- Создаст коммит
- Загрузит на GitHub

При первом запуске введите URL вашего репозитория, когда скрипт попросит.

### 4. Настройте GitHub Pages

1. Зайдите в ваш репозиторий на GitHub
2. **Settings** → **Pages** (в левом меню)
3. **Source**: выберите **"GitHub Actions"**
4. Сохраните

### 5. Готово! 🎉

Через 1-2 минуты сайт будет доступен по адресу:
- **`https://contour-archive.github.io`**

---

## 🔄 Автоматическое обновление

После **каждого изменения** на сайте просто запустите:

```powershell
.\deploy.ps1
```

Или вручную:
```powershell
git add .
git commit -m "Описание изменений"
git push
```

GitHub Actions автоматически обновит сайт через 1-2 минуты.

---

## 📝 Рекомендуемые названия репозитория

Для максимального погружения:

| Название репозитория | URL сайта |
|---------------------|-----------|
| `contour-archive` | `contour-archive.github.io` ✅ |
| `kontur-archive` | `kontur-archive.github.io` |
| `ktn-archive` | `ktn-archive.github.io` |
| `archive-kontur` | `archive-kontur.github.io` |

---

## 🛠️ Ручная настройка (если скрипт не работает)

```powershell
# Инициализация
git init
git add .
git commit -m "Initial commit: КОНТУР архив"

# Добавление удалённого репозитория (замените YOUR_USERNAME)
git remote add origin https://github.com/YOUR_USERNAME/contour-archive.git

# Переименование ветки
git branch -M main

# Загрузка
git push -u origin main
```

---

## ⚠️ Важные замечания

1. **Данные хранятся в браузере** (localStorage) — каждый пользователь видит свои данные
2. **Верификация email** работает в демо-режиме (код показывается в alert)
3. **Форум** доступен только верифицированным пользователям
4. **Секретные материалы** доступны только админу (`marcelo`)

---

## 🆘 Решение проблем

**Git не найден:**
- Установите Git: https://git-scm.com/download/win
- Перезапустите PowerShell

**Ошибка при push:**
- Используйте Personal Access Token вместо пароля
- GitHub → Settings → Developer settings → Personal access tokens → Generate new token
- Права: `repo`

**Сайт не обновляется:**
- Проверьте Actions в репозитории (вкладка "Actions")
- Убедитесь, что GitHub Pages настроен на "GitHub Actions"

---

**КОНТУР** — Архив контурных единиц

