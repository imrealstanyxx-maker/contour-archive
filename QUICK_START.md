# Быстрый старт — Развёртывание КОНТУР

## 🚀 Быстрая настройка (5 минут)

### 1. Установите Git (если ещё не установлен)
Скачайте: https://git-scm.com/download/win

### 2. Создайте репозиторий на GitHub
- Название: `contour-archive` (для URL: `contour-archive.github.io`)
- Или: `kontur-archive` (для URL: `kontur-archive.github.io`)
- Публичный репозиторий
- **НЕ** добавляйте README при создании

### 3. Запустите скрипт деплоя

В PowerShell в папке проекта:
```powershell
.\deploy.ps1
```

При первом запуске скрипт попросит URL вашего репозитория.

### 4. Настройте GitHub Pages

1. Зайдите в репозиторий → **Settings**
2. **Pages** (в левом меню)
3. **Source**: выберите **"GitHub Actions"**
4. Сохраните

### 5. Готово! 🎉

Сайт будет доступен через несколько минут по адресу:
- `https://contour-archive.github.io` (если репозиторий называется `contour-archive`)

---

## 📝 Обновление сайта

После любых изменений просто запустите:
```powershell
.\deploy.ps1
```

Или вручную:
```bash
git add .
git commit -m "Описание изменений"
git push
```

Сайт автоматически обновится через GitHub Actions.

---

## 🔧 Ручная настройка (если скрипт не работает)

```bash
git init
git add .
git commit -m "Initial commit: КОНТУР архив"
git branch -M main
git remote add origin https://github.com/YOUR_USERNAME/contour-archive.git
git push -u origin main
```

Затем настройте GitHub Pages в Settings → Pages → Source: GitHub Actions

