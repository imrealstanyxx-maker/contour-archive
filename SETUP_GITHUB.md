# 🚀 Настройка GitHub Pages для КОНТУР

## Пошаговая инструкция

### Шаг 1: Установка Git

1. Скачайте Git: https://git-scm.com/download/win
2. Установите с настройками по умолчанию
3. Перезапустите PowerShell/Terminal

### Шаг 2: Создание репозитория на GitHub

1. Зайдите на https://github.com и войдите
2. Нажмите **"+"** → **"New repository"**
3. **Repository name**: `contour-archive`
   - Это даст URL: `https://contour-archive.github.io`
   - Или используйте: `kontur-archive` → `kontur-archive.github.io`
4. **Description**: "Архив контурных единиц"
5. Выберите **Public**
6. **НЕ** ставьте галочки на README, .gitignore, license
7. Нажмите **"Create repository"**

### Шаг 3: Первая загрузка

Откройте PowerShell в папке проекта (`c:\Users\marcelo\Desktop\contour-site`) и выполните:

```powershell
# Инициализация
git init

# Добавление всех файлов
git add .

# Первый коммит
git commit -m "Initial commit: КОНТУР архив"

# Добавление удалённого репозитория (замените YOUR_USERNAME)
git remote add origin https://github.com/YOUR_USERNAME/contour-archive.git

# Переименование ветки
git branch -M main

# Загрузка
git push -u origin main
```

Введите ваш GitHub username и пароль (или токен доступа).

### Шаг 4: Настройка GitHub Pages

1. Зайдите в ваш репозиторий на GitHub
2. **Settings** (в верхнем меню)
3. В левом меню найдите **Pages**
4. В разделе **"Source"** выберите **"GitHub Actions"**
5. Сохраните (если нужно)

### Шаг 5: Проверка

Через 1-2 минуты сайт будет доступен по адресу:
- `https://contour-archive.github.io` (если репозиторий `contour-archive`)
- Или: `https://YOUR_USERNAME.github.io/contour-archive/`

---

## 🔄 Обновление сайта

После каждого изменения используйте скрипт:

```powershell
.\deploy.ps1
```

Или вручную:

```powershell
git add .
git commit -m "Описание изменений"
git push
```

GitHub Actions автоматически обновит сайт через 1-2 минуты.

---

## 📝 Рекомендации по названию

Для максимального погружения используйте одно из названий:

- `contour-archive` → `contour-archive.github.io` ✅
- `kontur-archive` → `kontur-archive.github.io`
- `ktn-archive` → `ktn-archive.github.io`
- `archive-kontur` → `archive-kontur.github.io`

---

## ⚠️ Важно

- Все данные хранятся в localStorage браузера (не на сервере)
- Каждый пользователь видит свои данные
- Для реального хранения данных нужен backend (в будущем)

---

## 🆘 Проблемы?

**Git не найден:**
- Установите Git: https://git-scm.com/download/win
- Перезапустите PowerShell

**Ошибка при push:**
- Проверьте правильность URL репозитория
- Используйте Personal Access Token вместо пароля (Settings → Developer settings → Personal access tokens)

**Сайт не обновляется:**
- Проверьте, что GitHub Actions включены (Settings → Actions)
- Проверьте статус деплоя в разделе "Actions" репозитория

